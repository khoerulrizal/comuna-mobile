# URL base API Comuna (comuna-web). Prefix EXPO_PUBLIC_ agar terbaca di client.
# Real device: pakai IP LAN Mac (lihat `ipconfig getifaddr en0`), BUKAN localhost.
# Simulator iOS boleh http://localhost:3000.
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.0:3000
